# Formora — 환경 변수 예시 (이 파일은 Git에 커밋해도 됩니다. 실제 값은 .env에만 넣으세요.)
#
# 복사: cp .env.example .env   (Windows: copy .env.example .env)
# .env 파일은 .gitignore에 의해 push되지 않습니다.

# VITE_* 변수만 클라이언트 번들에 노출됩니다. 비밀 API 키는 넣지 마세요.
# VITE_APP_NAME=Formora
